{
  "name": "mock-ai-products",
  "version": "1.2.0",
  "description": "SAP CAP + UI5 Product Management Application with Enhanced Features",
  "repository": "https://github.tools.sap/I335962/MockAI",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy",
    "test": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "@sap/cds": "^7.5.0",
    "@sap/cds-dk": "^7.5.0",
    "express": "^4.18.2",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "@sap/eslint-plugin-cds": "^2.6.3",
    "@sap/cds-lsp": "^7.5.0",
    "eslint": "^8.54.0",
    "jest": "^29.7.0",
    "sqlite3": "^5.1.6"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db/products.db"
        }
      }
    },
    "hana": {
      "deploy-format": "hdbtable"
    }
  }
}
